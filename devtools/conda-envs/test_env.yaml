name: test
channels:

  - defaults
  - conda-forge
  - bioconda

dependencies:
    # Base depends
  - python
  - pip
  - autodock-vina
  - openbabel

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
    - qcelemental
    - qcengine
    - pydantic
    - git+https://github.com/molssi/cmselemental.git@dev
    # mmelemental
    # mmic
    # mmic_translator
